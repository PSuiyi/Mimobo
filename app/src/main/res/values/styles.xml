<resources>

    <style name="AppSplash" parent="AppTheme">
        <item name="android:windowBackground">@mipmap/splash</item>
    </style>

    <!--通用按钮样式-->
    <style name="BtnStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">45dp</item>
        <item name="android:layout_marginLeft">@dimen/distance_view</item>
        <item name="android:layout_marginRight">@dimen/distance_view</item>
        <item name="android:gravity">center</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/switch_btn_bg_green</item>
        <item name="android:textSize">@dimen/title_larger_size</item>
    </style>

    <style name="BtnRedStyle" parent="BtnStyle">
        <item name="android:background">@drawable/bg_red</item>
    </style>

</resources>
